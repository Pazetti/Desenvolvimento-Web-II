import PromptSync = require('prompt-sync')

var prompt=PromptSync()

let n:number=Number(prompt("Digite Um numero: "))

if (n%2==0)
{
const mensagem=(prompt("Seu numero é par."))    
}
else
{
const mensagem2=(prompt("Seu numero é impar."))
}